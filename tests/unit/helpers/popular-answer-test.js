import { popularAnswer } from 'message-board/helpers/popular-answer';
import { module, test } from 'qunit';

module('Unit | Helper | popular answer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = popularAnswer([42]);
  assert.ok(result);
});
